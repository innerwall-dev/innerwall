// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: innerwall/v1/agent.proto

package innerwallv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	EnrollmentService_Enroll_FullMethodName = "/innerwall.v1.EnrollmentService/Enroll"
)

// EnrollmentServiceClient is the client API for EnrollmentService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type EnrollmentServiceClient interface {
	// Exchange a provisioning token and a certificate signing request for a
	// workload identity and signed credential.
	Enroll(ctx context.Context, in *EnrollRequest, opts ...grpc.CallOption) (*EnrollResponse, error)
}

type enrollmentServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewEnrollmentServiceClient(cc grpc.ClientConnInterface) EnrollmentServiceClient {
	return &enrollmentServiceClient{cc}
}

func (c *enrollmentServiceClient) Enroll(ctx context.Context, in *EnrollRequest, opts ...grpc.CallOption) (*EnrollResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(EnrollResponse)
	err := c.cc.Invoke(ctx, EnrollmentService_Enroll_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// EnrollmentServiceServer is the server API for EnrollmentService service.
// All implementations must embed UnimplementedEnrollmentServiceServer
// for forward compatibility.
type EnrollmentServiceServer interface {
	// Exchange a provisioning token and a certificate signing request for a
	// workload identity and signed credential.
	Enroll(context.Context, *EnrollRequest) (*EnrollResponse, error)
	mustEmbedUnimplementedEnrollmentServiceServer()
}

// UnimplementedEnrollmentServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedEnrollmentServiceServer struct{}

func (UnimplementedEnrollmentServiceServer) Enroll(context.Context, *EnrollRequest) (*EnrollResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Enroll not implemented")
}
func (UnimplementedEnrollmentServiceServer) mustEmbedUnimplementedEnrollmentServiceServer() {}
func (UnimplementedEnrollmentServiceServer) testEmbeddedByValue()                           {}

// UnsafeEnrollmentServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to EnrollmentServiceServer will
// result in compilation errors.
type UnsafeEnrollmentServiceServer interface {
	mustEmbedUnimplementedEnrollmentServiceServer()
}

func RegisterEnrollmentServiceServer(s grpc.ServiceRegistrar, srv EnrollmentServiceServer) {
	// If the following call panics, it indicates UnimplementedEnrollmentServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&EnrollmentService_ServiceDesc, srv)
}

func _EnrollmentService_Enroll_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(EnrollRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EnrollmentServiceServer).Enroll(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EnrollmentService_Enroll_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EnrollmentServiceServer).Enroll(ctx, req.(*EnrollRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// EnrollmentService_ServiceDesc is the grpc.ServiceDesc for EnrollmentService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var EnrollmentService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "innerwall.v1.EnrollmentService",
	HandlerType: (*EnrollmentServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Enroll",
			Handler:    _EnrollmentService_Enroll_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "innerwall/v1/agent.proto",
}

const (
	AgentService_Sync_FullMethodName            = "/innerwall.v1.AgentService/Sync"
	AgentService_ReportFlows_FullMethodName     = "/innerwall.v1.AgentService/ReportFlows"
	AgentService_RenewCredential_FullMethodName = "/innerwall.v1.AgentService/RenewCredential"
)

// AgentServiceClient is the client API for AgentService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type AgentServiceClient interface {
	// The desired-state sync stream. Lifecycle:
	//  1. Agent connects and sends Hello (agent info, host facts, last
	//     applied policy version — 0 if none).
	//  2. Server replies HelloAck (sync configuration), then unconditionally
	//     sends a PolicySnapshot. Deltas are only ever sent within a live
	//     stream, after the snapshot; reconnection always restarts from a
	//     snapshot. This trades a few kilobytes on reconnect for the
	//     elimination of cross-session divergence as a failure class.
	//  3. Server pipelines PolicyUpdates without waiting for acks. The agent
	//     applies updates strictly in order and acks each one. Apply is
	//     atomic: on failure the agent remains on its last good version and
	//     acks FAILED, and the server's recovery move is a fresh snapshot,
	//     never a retried delta.
	//
	// Low-volume control traffic only. Flow telemetry is deliberately a
	// separate RPC so that reporting volume can never head-of-line-block a
	// policy update.
	Sync(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[SyncRequest, SyncResponse], error)
	// High-volume, loss-tolerant flow telemetry. The agent aggregates flows
	// over the window configured in SyncConfig and uploads batches. The
	// response is returned when the client closes the stream.
	ReportFlows(ctx context.Context, opts ...grpc.CallOption) (grpc.ClientStreamingClient[ReportFlowsRequest, ReportFlowsResponse], error)
	// Rotate the workload credential before expiry. Authenticated by the
	// current credential; the renewed credential binds the same identity.
	RenewCredential(ctx context.Context, in *RenewCredentialRequest, opts ...grpc.CallOption) (*RenewCredentialResponse, error)
}

type agentServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAgentServiceClient(cc grpc.ClientConnInterface) AgentServiceClient {
	return &agentServiceClient{cc}
}

func (c *agentServiceClient) Sync(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[SyncRequest, SyncResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &AgentService_ServiceDesc.Streams[0], AgentService_Sync_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[SyncRequest, SyncResponse]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AgentService_SyncClient = grpc.BidiStreamingClient[SyncRequest, SyncResponse]

func (c *agentServiceClient) ReportFlows(ctx context.Context, opts ...grpc.CallOption) (grpc.ClientStreamingClient[ReportFlowsRequest, ReportFlowsResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &AgentService_ServiceDesc.Streams[1], AgentService_ReportFlows_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[ReportFlowsRequest, ReportFlowsResponse]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AgentService_ReportFlowsClient = grpc.ClientStreamingClient[ReportFlowsRequest, ReportFlowsResponse]

func (c *agentServiceClient) RenewCredential(ctx context.Context, in *RenewCredentialRequest, opts ...grpc.CallOption) (*RenewCredentialResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RenewCredentialResponse)
	err := c.cc.Invoke(ctx, AgentService_RenewCredential_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AgentServiceServer is the server API for AgentService service.
// All implementations must embed UnimplementedAgentServiceServer
// for forward compatibility.
type AgentServiceServer interface {
	// The desired-state sync stream. Lifecycle:
	//  1. Agent connects and sends Hello (agent info, host facts, last
	//     applied policy version — 0 if none).
	//  2. Server replies HelloAck (sync configuration), then unconditionally
	//     sends a PolicySnapshot. Deltas are only ever sent within a live
	//     stream, after the snapshot; reconnection always restarts from a
	//     snapshot. This trades a few kilobytes on reconnect for the
	//     elimination of cross-session divergence as a failure class.
	//  3. Server pipelines PolicyUpdates without waiting for acks. The agent
	//     applies updates strictly in order and acks each one. Apply is
	//     atomic: on failure the agent remains on its last good version and
	//     acks FAILED, and the server's recovery move is a fresh snapshot,
	//     never a retried delta.
	//
	// Low-volume control traffic only. Flow telemetry is deliberately a
	// separate RPC so that reporting volume can never head-of-line-block a
	// policy update.
	Sync(grpc.BidiStreamingServer[SyncRequest, SyncResponse]) error
	// High-volume, loss-tolerant flow telemetry. The agent aggregates flows
	// over the window configured in SyncConfig and uploads batches. The
	// response is returned when the client closes the stream.
	ReportFlows(grpc.ClientStreamingServer[ReportFlowsRequest, ReportFlowsResponse]) error
	// Rotate the workload credential before expiry. Authenticated by the
	// current credential; the renewed credential binds the same identity.
	RenewCredential(context.Context, *RenewCredentialRequest) (*RenewCredentialResponse, error)
	mustEmbedUnimplementedAgentServiceServer()
}

// UnimplementedAgentServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAgentServiceServer struct{}

func (UnimplementedAgentServiceServer) Sync(grpc.BidiStreamingServer[SyncRequest, SyncResponse]) error {
	return status.Error(codes.Unimplemented, "method Sync not implemented")
}
func (UnimplementedAgentServiceServer) ReportFlows(grpc.ClientStreamingServer[ReportFlowsRequest, ReportFlowsResponse]) error {
	return status.Error(codes.Unimplemented, "method ReportFlows not implemented")
}
func (UnimplementedAgentServiceServer) RenewCredential(context.Context, *RenewCredentialRequest) (*RenewCredentialResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RenewCredential not implemented")
}
func (UnimplementedAgentServiceServer) mustEmbedUnimplementedAgentServiceServer() {}
func (UnimplementedAgentServiceServer) testEmbeddedByValue()                      {}

// UnsafeAgentServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AgentServiceServer will
// result in compilation errors.
type UnsafeAgentServiceServer interface {
	mustEmbedUnimplementedAgentServiceServer()
}

func RegisterAgentServiceServer(s grpc.ServiceRegistrar, srv AgentServiceServer) {
	// If the following call panics, it indicates UnimplementedAgentServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AgentService_ServiceDesc, srv)
}

func _AgentService_Sync_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(AgentServiceServer).Sync(&grpc.GenericServerStream[SyncRequest, SyncResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AgentService_SyncServer = grpc.BidiStreamingServer[SyncRequest, SyncResponse]

func _AgentService_ReportFlows_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(AgentServiceServer).ReportFlows(&grpc.GenericServerStream[ReportFlowsRequest, ReportFlowsResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type AgentService_ReportFlowsServer = grpc.ClientStreamingServer[ReportFlowsRequest, ReportFlowsResponse]

func _AgentService_RenewCredential_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RenewCredentialRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AgentServiceServer).RenewCredential(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AgentService_RenewCredential_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AgentServiceServer).RenewCredential(ctx, req.(*RenewCredentialRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AgentService_ServiceDesc is the grpc.ServiceDesc for AgentService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AgentService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "innerwall.v1.AgentService",
	HandlerType: (*AgentServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "RenewCredential",
			Handler:    _AgentService_RenewCredential_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Sync",
			Handler:       _AgentService_Sync_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
		{
			StreamName:    "ReportFlows",
			Handler:       _AgentService_ReportFlows_Handler,
			ClientStreams: true,
		},
	},
	Metadata: "innerwall/v1/agent.proto",
}
